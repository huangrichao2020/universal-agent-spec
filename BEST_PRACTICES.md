# Agent 工程最佳实践 30 条

> 从 42 章知识中提炼的工程断言，每条可独立引用。

## 基础层 (1-10)

1. **默认无状态，不要假设模型会记住你** — 除非应用显式回灌上下文，否则 LLM API 不保留记忆。(→ ch.00, ch.01)
2. **一次调用只是执行窗口，不是整个 Agent** — 真正持续存在的是外层状态、工具和记忆系统。(→ ch.01, ch.03)
3. **Skill 是可复用的操作资产，不是 prompt 分类目录** — 它必须封装流程、约束和质量门禁。(→ ch.02)
4. **Agent 的护城河是领域记忆，不是提示词花样** — 模型可以租，经过验证的行业记忆和流程很难复制。(→ ch.03, ch.11)
5. **优先复用成熟壳程序，再考虑自研运行时** — 官方 CLI 往往已经替你解决了工具调用、错误处理和 token 管理。(→ ch.04)
6. **工作流稳定性比模型聪明更重要** — 流程边界和交接契约错了，换更强模型也救不了系统。(→ ch.05)
7. **交接手册要写成下一位执行者可继续运行的状态摘要** — 讲故事没有价值，阻塞点、下一步和风险才有价值。(→ ch.06)
8. **监控默认要便宜，只有异常才升级** — 感知层的职责是分类和升级，不是把监控也做成重 Agent。(→ ch.07)
9. **多 Agent 更像流水线，不像群聊** — 每个 Agent 只应接收清晰物料、完成清晰工位、交付清晰产物。(→ ch.08)
10. **部署模式是产品决策，不是运维尾声** — 本地、云端和混合方案本质上是在取舍数据主权、成本和体验。(→ ch.09)

## 模式层 (11-20)

11. **默认从轻 Agent 起步，只有用户价值明确依赖持续 AI 判断时才加重**。(→ ch.10)
12. **ToB Agent 卖的是“专家脑”的可复制运行时，而不是模型调用次数**。(→ ch.11)
13. **工具描述比系统提示词更影响可靠性** — 少而准的工具，通常优于多而糊的工具。(→ ch.12)
14. **先用 CoT，再按价值引入更重的推理结构** — ToT 和 GoT 只该服务高价值、高搜索复杂度的问题。(→ ch.13)
15. **任何生产 ReAct 循环都必须有步数、时间和审批边界** — 否则它迟早变成失控循环。(→ ch.14)
16. **Plan-and-Execute 只该用在足够长、足够复杂的任务上** — 简单任务硬加规划层，只会徒增开销。(→ ch.15)
17. **在自建工具协议之前，先看 MCP 能不能直接解决问题** — 先用标准化连接，再谈私有封装。(→ ch.16)
18. **把确定性操作交给工具，把开放性判断交给 Agent** — 这是 A2A 与工具调用的根本分界线。(→ ch.17)
19. **如果前端只会流式输出文本，它就没有把 Agent 的真实运行过程暴露给用户**。(→ ch.18)
20. **任务形状决定协议栈、框架和架构，而不是热度榜单** — 先问任务怎么失败，再问该用哪套工具。(→ ch.19, ch.20, ch.29)

## 生产层 (21-30)

21. **低代码减少的是手写代码，不是系统设计责任** — 状态、权限、发布和回滚纪律依然存在。(→ ch.21)
22. **多 Agent 架构默认先从 Supervisor 做起** — 只有单一监督者管不过来时，才引入更复杂的层级或群体结构。(→ ch.22)
23. **图编排是 Agent 系统的汇编语言** — 只有当条件路由、检查点和恢复真的重要时，才值得上图。(→ ch.23)
24. **记忆必须是受治理的数据管道，而不是原始对话堆积** — 未经验证的写入会持续污染未来运行。(→ ch.24)
25. **先加可观测性，再加功能** — 没有链路日志、成本归因和中间步骤，Agent bug 会难十倍定位。(→ ch.25)
26. **护栏是运行时策略层，不只是输入过滤** — 执行限制、输出校验、审批门和回滚路径都要落地。(→ ch.26)
27. **真正有竞争力的团队把评估做成持续系统，而不是上线前一次性跑分**。(→ ch.27)
28. **公开 benchmark 最有价值的产出常常不是总分，而是失败簇分析** — 你的私有工作负载才是最终裁判。(→ ch.27, ch.39)
29. **能走 API 就不要走屏幕；一旦任务变成长程自治，就必须同时设计 checkpoint、sandbox 和 cost routing**。(→ ch.32, ch.35, ch.36, ch.37)
30. **生产级成功不是“完全替代人”，而是让失败显而易见、责任归属清晰、执行外壳真正适配任务**。(→ ch.28, ch.38, ch.40, ch.41)
